name: Release Workflow

on:
  workflow_dispatch:
    inputs:
      version_type:
        description: "Version bump"
        required: true
        type: choice
        options:
          - major
          - minor
          - patch

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:

      # ✅ Checkout DEV branch with full history
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: dev

      - name: Configure Git
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"

      # ✅ Ensure version file exists
      - name: Ensure version file exists
        run: |
          if [ ! -f version.txt ]; then
            echo "0.0.0" > version.txt
          fi

      # ✅ Read current version
      - name: Read current version
        id: current
        run: |
          VERSION=$(cat version.txt)
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      # ✅ Calculate new version
      - name: Calculate new version
        id: bump
        run: |
          VERSION=${{ steps.current.outputs.version }}
          TYPE=${{ github.event.inputs.version_type }}

          IFS='.' read -r MAJOR MINOR PATCH <<< "$VERSION"

          if [ "$TYPE" = "major" ]; then
            MAJOR=$((MAJOR+1))
            MINOR=0
            PATCH=0
          elif [ "$TYPE" = "minor" ]; then
            MINOR=$((MINOR+1))
            PATCH=0
          else
            PATCH=$((PATCH+1))
          fi

          NEW_VERSION="$MAJOR.$MINOR.$PATCH"
          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT

      # ✅ Generate changelog BEFORE creating branch/tag (CRITICAL FIX)
      - name: Generate changelog
        run: |
          echo "## v${{ steps.bump.outputs.new_version }} - $(date)" >> CHANGELOG.md
          echo "" >> CHANGELOG.md

          LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")

          if [ -z "$LAST_TAG" ]; then
            echo "No previous tag found, using all commits"
            git log --pretty=format:"- %s" >> CHANGELOG.md
          else
            echo "Using commits since $LAST_TAG"
            git log $LAST_TAG..HEAD --pretty=format:"- %s" >> CHANGELOG.md
          fi

          echo "" >> CHANGELOG.md

      # ✅ Create release branch safely
      - name: Create release branch
        run: |
          BRANCH="release_v${{ steps.bump.outputs.new_version }}"

          # Delete remote branch if exists (avoid conflict)
          git push origin --delete $BRANCH || true

          git checkout -b $BRANCH

      # ✅ Update version file
      - name: Update version file
        run: |
          echo "${{ steps.bump.outputs.new_version }}" > version.txt

      # ✅ Commit changes
      - name: Commit release files
        run: |
          git add version.txt CHANGELOG.md
          git commit -m "Release v${{ steps.bump.outputs.new_version }}"

      # ✅ Create tag AFTER changelog (important)
      - name: Create Git tag
        run: |
          git tag v${{ steps.bump.outputs.new_version }}

      # ✅ Push branch + tag
      - name: Push branch and tag
        run: |
          git push origin HEAD
          git push origin v${{ steps.bump.outputs.new_version }}
